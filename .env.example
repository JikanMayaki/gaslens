# =============================================================================
# GasLens Environment Variables
# =============================================================================
# IMPORTANT: Variables WITHOUT "NEXT_PUBLIC_" prefix are server-side only
# and will NOT be exposed to the browser (more secure for API keys)
# =============================================================================

# -----------------------------------------------------------------------------
# Server-Side API Keys (SECURE - not exposed to browser)
# -----------------------------------------------------------------------------
# These are used in API routes only, never sent to the client

# Etherscan API - Required for gas price data
# Get your key at: https://etherscan.io/apis
ETHERSCAN_API_KEY=your_etherscan_key_here

# 0x API - For real swap quotes (RECOMMENDED)
# Get your key at: https://0x.org/docs/api
ZEROX_API_KEY=your_0x_key_here

# 1inch API - Optional, for additional swap quotes
# Get your key at: https://portal.1inch.dev/ (requires signup)
# ONEINCH_API_KEY=your_1inch_key_here

# CoinGecko API - Optional (free tier works WITHOUT a key)
# Only needed for higher rate limits
# COINGECKO_API_KEY=your_coingecko_key_here

# -----------------------------------------------------------------------------
# Client-Side Variables (exposed to browser - only non-sensitive data)
# -----------------------------------------------------------------------------

# WalletConnect Project ID (required for wallet connection)
# Get one at: https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id

# Treasury Wallet Address - Wallet that receives crypto payments
# IMPORTANT: Use a secure multisig wallet for production
NEXT_PUBLIC_TREASURY_WALLET_ADDRESS=your_treasury_wallet_address

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=GasLens

# -----------------------------------------------------------------------------
# Optional Configuration
# -----------------------------------------------------------------------------

# Ethereum RPC URL (optional, for direct blockchain queries)
# You can use Infura, Alchemy, or your own node
# NEXT_PUBLIC_ETHEREUM_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your-api-key
